#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../uploadcare/bin/uploadcare.js" "$@"
  ret=$?
else 
  node  "$basedir/../uploadcare/bin/uploadcare.js" "$@"
  ret=$?
fi
exit $ret
